{
  "name": "kidscade",
  "private": true,
  "scripts": {
    "build": "node scripts/build-cloudflare.cjs && node scripts/inject-game-integrations.cjs",
    "build:cloudflare": "node scripts/build-cloudflare.cjs && node scripts/inject-game-integrations.cjs",
    "dev:cloudflare": "npm run build:cloudflare && wrangler dev",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler deploy"
  },
  "devDependencies": {
    "sharp": "0.35.4",
    "wrangler": "^4.0.0"
  }
}
