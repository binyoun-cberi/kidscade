{
  "name": "kidscade",
  "private": true,
  "scripts": {
    "build": "node scripts/build-cloudflare.cjs",
    "build:cloudflare": "node scripts/build-cloudflare.cjs",
    "dev:cloudflare": "npm run build:cloudflare && wrangler dev",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
